[package]
name = "reitero_video_common"
version = "0.1.0"
edition = "2024"

[lib]
name = "reitero_video_common"
crate-type = ["rlib"]

[features]
default = ["simd"]
simd = ["wide"]

[dependencies]
wide = { version = "1.1", optional = true }
yuv = "0.8"

